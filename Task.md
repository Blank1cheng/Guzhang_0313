# 平台迭代 Task

最后更新：2026-03-17

## 使用规则
- 每一轮先从“甲方视角”补充问题和验收口径。
- 再从“设计 / 开发视角”落地实现并勾选完成项。
- 已完成项使用 `[x]`，未完成项使用 `[ ]`。
- 后续继续在这个文件上滚动更新，不新开零散说明文档。

## 甲方视角：当前重点问题
- [x] 完整模块库原来使用覆盖式抽屉，打开后无法直接拖拽到主界面画布。
- [x] 完整模块库窗口过小，即便可滚动也不适合高频浏览和拖放。
- [x] 主界面存在历史乱码和局部文案截断，影响专业感。
- [x] 生成页更像说明页，不像建模工作台。
- [x] 执行页和分析页与主界面的视觉语言不统一。
- [x] 主界面要保证整体不滚动，但局部列表和日志允许滚动。
- [x] 入口标题、运行日志、属性面板、模板词典仍有坏编码残留，必须彻底清理。
- [x] 主界面左侧那组统计卡占用模块库空间，应移除并让位给库区浏览。
- [x] 生成页右侧预览卡存在内容堆叠，模板摘要还出现 `undefined / undefined`。
- [ ] 继续验收 1920 / 1440 / 1280 宽度下的标题、按钮、标签密度。
- [ ] 继续精修 LogicFlow 节点皮肤、默认排布和连线走向。
- [ ] 继续提升主界面库区和画布区的拖拽反馈细节。

## 设计 / 开发视角：已完成
- [x] 建立四界面结构：主界面、故障生成界面、执行界面、总结分析界面。
- [x] 主界面左侧改为 Simulink 式模块浏览器，支持故障模板 / 基础模块分段浏览。
- [x] 将“完整模块库”从覆盖抽屉改为主界面内可展开的停靠式浏览器，修复拖拽阻断。
- [x] 拖拽模块和模板时，统一切回主界面并确保画布可接收拖放。
- [x] 主界面底部大摘要区已移除，系统总布图获得更大显示面积。
- [x] 主界面左栏统计卡已移除，空间全部让给模块库与模板浏览。
- [x] 画布增加空态、拖拽态、放置态三种引导反馈。
- [x] 执行页重构为“运行控制 + KPI + 示波器 + 任务状态 + 日志控制台”。
- [x] 分析页重构为“KPI + 对比曲线 + 诊断摘要 + 任务复盘”。
- [x] 任务配置抽屉重构为单列配置流，适配 520px 抽屉宽度。
- [x] 故障生成页侧栏改成导轨式结构，压缩说明文字，突出保存、载入和建模节奏。
- [x] 重写 `TemplateModelingPanel`、`ExecutionPanel`、`AnalysisPanel`、`InjectionTaskPanel`，统一版式和中文文案。
- [x] 重写 `PropertyInspector`，修复节点属性面板全部乱码并统一交互表单。
- [x] 重写 `useSimulationRuntime` 的用户可见文案，修复日志、状态、曲线名、速度选项乱码。
- [x] 重写 `faultCatalog.js` 与 `labels.js`，统一故障词典、层级、方法、预设、模块标签。
- [x] 修复 `App.vue` 历史坏标签和损坏字符串，恢复编译和中文显示。
- [x] 修复 `index.html` 标题编码，并将关键文件统一为 UTF-8 无 BOM。
- [x] 修复生成页模板摘要引用错误，消除 `undefined / undefined`。
- [x] 精简生成页预览卡和侧栏摘要，消除右侧图表与指标区重叠。
- [x] `npm run build` 构建通过。

## 下一轮待实现
- [ ] 对 LogicFlow 节点做一轮更彻底的工业化皮肤精修。
- [ ] 继续压缩主界面右侧配置区的阅读负担。
- [ ] 做一轮默认节点位置和连线折线路径的专项优化。
- [ ] 继续做响应式巡检，重点看 1280 以下布局降级。
- [ ] 评估 `logicflow / echarts / antd` 的分包策略，降低构建告警。

## 本轮验收记录
### 2026-03-17 第 11 轮
- 以 `5181` 开发端口为准做真实页面验收，不再只看 `4181` 预览页。
- 移除主界面左栏统计卡，左侧空间完整让给模块库浏览和模板操作。
- 重构生成页侧栏为“当前模板 + 最近模板”两段式，去掉额外步骤块，避免纵向拥挤。
- 精简 `TemplateModelingPanel` 顶部摘要和右侧预览卡，移除重复指标，修复图表与下方模板列表重叠。
- 修复生成页模板摘要计算错误，当前模板描述恢复为正确中文。
- 使用 Edge 通道对 `5181` 页面做了截图验收，至少检查了总览、生成、执行、分析四个界面在 `1600x980` 与 `1366x900` 下的渲染结果。
- 已重新执行本地构建验收：`npm run build` 通过。
